# Markdown2PDF

A responsive, client-side web application to convert Markdown text into high-quality PDF documents using `marked` and `html2pdf.js`. Built entirely on the client-side with zero server components required!
